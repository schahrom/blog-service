env: "local" # local, dev, prod
storage_url: "postgresql://postgres:postgres@localhost:5433/postgres?sslmode=disable"
http_server:
  address: "localhost:8082"
  timeout: 4s
  idle_timeout: 60s
security:
  token_ttl: 72000